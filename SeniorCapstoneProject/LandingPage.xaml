<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SeniorCapstoneProject.LandingPage"
             Title="Home">

    <Grid RowDefinitions="Auto,*">
        <Grid Grid.Row="0" Padding="10,10,10,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <!-- Welcome, User Label -->
            <Label x:Name="WelcomeLabel" Text="Welcome," FontSize="24" VerticalOptions="Center" HorizontalOptions="Start"/>
            
            <ImageButton Source="profile_icon.png"
                         x:Name="ProfileButton"
                         Clicked="OnProfileClicked"
                         WidthRequest="40"
                         HeightRequest="40"
                         BackgroundColor="Transparent"
                         HorizontalOptions="End"
                         VerticalOptions="Center"
                         Grid.Column="1"/>
        </Grid>

        <Grid Grid.Row="1" Padding="20" RowSpacing="20" ColumnSpacing="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Row="0" Grid.Column="0" Text="Chat" Clicked="OnChatClicked"/>
            <Button Grid.Row="0" Grid.Column="1" Text="History" Clicked="OnHistoryClicked"/>
            <Button Grid.Row="1" Grid.Column="0" Text="Settings" Clicked="OnSettingsClicked"/>
            <Button Grid.Row="1" Grid.Column="1" Text="Help" Clicked="OnHelpClicked"/>
        </Grid>
    </Grid>
</ContentPage>